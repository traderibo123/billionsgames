# Billions — Real vs Fake Swipe (v3.5 full)
Start → Game → Game Over flow; fast images via CDN 256×256; staged preload & concurrency; timeout+fallback & warm swap; SW caching; diagnostics; demo mode.
